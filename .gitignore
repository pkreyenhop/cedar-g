# Build output (leading slash = repo root only, so cmd/<name>/ is NOT ignored)
/bin/
/tiogaviewer
/xeroxdl
/xeroxsrc

# Downloaded archive mirrors (download-src is checked in intentionally)
/download/
/mirror/

# Local tooling / editor / OS
/.claude/
.DS_Store
