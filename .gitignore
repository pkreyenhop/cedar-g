# Build output (leading slash = repo root only, so cmd/<name>/ is NOT ignored)
/bin/
/tiogaviewer
/xeroxdl
/xeroxsrc

# Downloaded archive mirrors
/download/
/download-src/
/mirror/

# Local tooling / editor / OS
/.claude/
.DS_Store
